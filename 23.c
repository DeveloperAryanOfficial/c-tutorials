#include<stdio.h>
int main ()
{
    int marks[4][4];

    // This is the example of 2d array
    /*
    for (int i = 0; i < 4; i++)
    {
        for (int j = 0; j < 4; j++)
        {
            printf("Enter the %d row of the matrix \n",i+1);
            scanf("%d",&marks[i][j]);
        }
        
    }

    for (int i = 0; i < 4; i++)
    {
        for (int j = 0; j < 4; j++)
        {
            printf("%d",marks[i][j]);
        }
        printf("\n");
        
    }
    */
    


   // This is the single dimension array
   /*
    marks[0]=34;
    marks[1]=35;
    marks[2]=36;
    marks[3]=37;

    printf("Marks of student 1 is : %d.\n",marks[0]);
    marks[0]=76;
    printf("Marks of student 1 is : %d.",marks[0]);

    for (int i = 0; i < 4; i++)
    {
        printf("Enter the value of %d element of the array\n",i);
        scanf("%d",&marks[i]);
    }

    for (int i = 0;  i < 4 ; i++)
    {
        printf("The value of %d element is %d.",i,marks[i]);
        printf("\n");
    }
    */


    
    
return 0;
}